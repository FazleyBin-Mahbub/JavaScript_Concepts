// const amarRoll = 567;
// const taharRoll = 568;
/* 
if (amarRoll > taharRoll) {
  console.log(`${amarRoll} besi 8) `);
} else {
  console.log(`${taharRoll} besi xD`);
}

 */
 /*    using math operation 
 var maximum = Math.max(amarRoll, taharRoll);
 console.log(maximum);
  */

/* //  using function 
function findLargest(num1, num2, num3) {
  if (num1 > num2 && num1 > num3) {
    return `${num1} is bigger`;
  } else if (num2 > num1 && num2 > num3) {
    return `${num2} is bigger`;
  } else {
    return `${num3} is big number`;
  }
}

var large = findLargest(2, 3, 4);
console.log(large); 

*/
